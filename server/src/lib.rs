struct ServerHelper {
    port: String,
}

/// listening to the request, and be able to send many response at one time
pub struct GraphqlServer {}

impl GraphqlServer {
    pub fn new() -> Self {
        Self {}
    }
}
